# Flow-RS Interactive Playground Example
#
# This example demonstrates a complete graph editing interface with all Flow-RS capabilities.
# It uses wasm-pack for building instead of trunk.
#
# Build command: wasm-pack build --target web --out-dir pkg --dev
# Serve command: python3 -m http.server 8000
# Open: http://localhost:8000
